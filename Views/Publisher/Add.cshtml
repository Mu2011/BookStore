@model Mu_BookStore.Models.Domain.Publisher
@{
}

<div class="container pt-5">
  <div class="row">
    <div class="col-md-6">
      <h3>Add Publisher</h3>
      <form asp-action="Add" method="post">
        <div class="from-group">
          <label asp-for="PublisherName">Name *</label>
          <input type="text" asp-for="PublisherName" class="form-control" />
          <span asp-validation-for="PublisherName" class="text-danger"></span>
        </div>
        @if (TempData["msg"] != null)
        {
          <div class="from-group">
            <span class="text-dark">@TempData["msg"]</span>
          </div>
        }
        <div class="from-group pt-3">
          <button type="submit" class="btn btn-dark"><i class="bi bi-save"></i> Save</button>
          <a href="/Publisher/GetAll" class="btn btn-primary"><i class="bi bi-list"></i> List</a>
        </div>
      </form>
    </div>
  </div>
</div>